#include <string>
#include <cmath>

#ifndef COORDINATEFUNC_HPP
#define COORDINATEFUNC_HPP

std::string getField(std::string location, int field);
double getLongitude(std::string longit);
double getLatitude(std::string latit);

#endif