#include <string>
#include <cmath>


#ifndef GENERALFUNC_HPP
#define GENERALFUNC_HPP
#include "Destination.hpp"
#include "coordinatefunc.hpp"
double toRadians(double deg); //toRadians is a general trigonometric function not related to destination struct or its functions
void distanceloop(void); //Main method interfaces that asks for all location inputs and calculates the distances 
// and outputs the highest and lowest distances from the starting location in the output format

#endif

